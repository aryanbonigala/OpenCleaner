"""Central caps for filesystem scans (v0.2 hardening)."""

from __future__ import annotations

# Per-root walk limits
TEMP_MAX_FILES = 500
TEMP_MAX_DEPTH = 3
TEMP_MAX_TOTAL_BYTES = 256 * 1024 * 1024
TEMP_TIMEOUT_S = 25.0

DOWNLOADS_MAX_FILES = 400
DOWNLOADS_MAX_DEPTH = 2
DOWNLOADS_MAX_TOTAL_BYTES = 512 * 1024 * 1024
DOWNLOADS_TIMEOUT_S = 35.0

DESKTOP_MAX_FILES = 300
DESKTOP_MAX_DEPTH = 2
DESKTOP_MAX_TOTAL_BYTES = 256 * 1024 * 1024
DESKTOP_TIMEOUT_S = 20.0

LARGE_FILE_SCAN_MAX_FILES = 800
LARGE_FILE_SCAN_MAX_DEPTH = 12
LARGE_FILE_SCAN_MAX_TOTAL_BYTES = 768 * 1024 * 1024
LARGE_FILE_SCAN_TIMEOUT_S = 45.0
LARGE_FILE_THRESHOLD_BYTES = 750 * 1024 * 1024

BROWSER_ROOT_MAX_FILES = 50_000
BROWSER_ROOT_MAX_DEPTH = 24
BROWSER_ROOT_MAX_TOTAL_BYTES = 1_500 * 1024 * 1024
BROWSER_ROOT_TIMEOUT_S = 60.0

ORPHAN_DIR_CHECK_MAX_FILES = 4
ORPHAN_DIR_CHECK_MAX_DEPTH = 32
ORPHAN_DIR_CHECK_MAX_TOTAL_BYTES = 64 * 1024 * 1024
STARTUP_FOLDER_MAX_FILES = 400
STARTUP_FOLDER_MAX_DEPTH = 3
STARTUP_FOLDER_MAX_BYTES = 128 * 1024 * 1024
STARTUP_FOLDER_TIMEOUT_S = 15.0

# Duplicates: never hash more than this per file; skip hashing if file larger
MAX_DUPLICATE_HASH_BYTES = 4 * 1024 * 1024
MAX_DUPLICATE_FILE_SIZE_TO_HASH_BYTES = 256 * 1024 * 1024
